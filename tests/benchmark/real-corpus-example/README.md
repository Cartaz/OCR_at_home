# Real corpus template

Copy `manifest.example.json` to a private/local corpus directory as `manifest.json`, place the referenced images beside it, and replace each `expected_text` value with an exact human transcription.

Real documents are intentionally not committed here: the benchmark runner accepts any external directory through `--corpus-dir`.
