PySide6>=6.6.0
Pillow>=10.0.0
numpy>=1.26.0
huggingface_hub>=0.27.0
PyMuPDF>=1.24.0

# llama-server è un runtime esterno, non un pacchetto Python.
# Su Arch/CachyOS il pacchetto ufficiale è `llama-cpp`.
# Per la build SYCL locale e riproducibile usare ./install.sh.
